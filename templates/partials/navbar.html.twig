<div class="uk-section uk-padding-remove-vertical uk-background-secondary uk-light" uk-sticky="animation: uk-animation-slide-top; sel-target: .uk-container; cls-active: uk-navbar-sticky; cls-inactive: uk-navbar-transparent uk-light; top: 200">
    <div class="uk-container uk-container-small">
        <nav class="uk-navbar-container uk-navbar-transparent uk-margin" uk-navbar>
            <div class="uk-navbar-left">
                <a class="uk-navbar-item uk-logo" href="#">eEnglish</a>
                <ul class="uk-navbar-nav">
                    <li><a href="#"><span class="uk-icon uk-margin-small-right" uk-icon="icon: star"></span>Contact</a></li>
                </ul>
                <div class="uk-navbar-item"></div>
            </div>
            <div class="uk-navbar-right">
                {% if is_granted('ROLE_USER') %}
                <ul class="uk-navbar-nav">
                    <li>
                        <a href="#"><span class="uk-icon uk-margin-small-right" uk-icon="icon: star"></span>{{ app.user.forname }}</a>
                        <div class="uk-navbar-dropdown">
                            <ul class="uk-nav uk-navbar-dropdown-nav">
                                <li><a href="#"><span uk-icon="happy"></span> {{ app.user.forname }} {{ app.user.name }}</a></li>
                                <li><a href="{{ path('app_logout') }}"><span uk-icon="sign-out"></span> Déconnexion</a></li>
                            </ul>
                        </div>
                    </li>
                </ul>
                {% else %}
                <ul class="uk-navbar-nav">
                    <li>
                        <a href="#" uk-icon="icon: happy"></a>
                        <div class="uk-navbar-dropdown">
                            <ul class="uk-nav uk-navbar-dropdown-nav">
                                <li><a href="{{ path('app_login') }}"><span uk-icon="sign-in"></span> Connection</a></li>
                                <li><a href="{{ path('app_register') }}"><span uk-icon="settings"></span> Enregistrement</a></li>
                                <li><a href="#"><span uk-icon="unlock"></span> Mot de passe</a></li>
                            </ul>
                        </div>
                    </li>
                </ul>
                {% endif %}
            </div>
        </nav>
    </div>
</div>